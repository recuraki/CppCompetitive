#include <bits/stdc++.h>
using namespace std;
using ll = long long int;
#define ALL(x) (x).begin(), (x).end()
#define FOR(i, begin, end) for(ll i=(begin),i##_end_=(end);i<i##_end_;i++)
#define IFOR(i, begin, end) for(ll i=(end)-1,i##_begin_=(begin);i>=i##_begin_;i--)
#define REP(i, n) FOR(i,0,n)

#define clkstart start = clock();
#define clkend  end = clock();std::cout << "duration = " << (double)(end - start) / CLOCKS_PER_SEC << "sec.\n";
clock_t start, end1;
int main(){
    auto a = 1e9;

}